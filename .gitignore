# --- DevOps habit: never commit junk, secrets, or generated files ---

# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# Generated data outputs (we regenerate these by running the scripts)
data/clean_sales.csv
data/churn.csv

# Trained model binaries (regenerated by 02_train.py; we don't version binaries)
projects/02_churn_prediction/model/churn_model.joblib

# Jupyter
.ipynb_checkpoints/

# OS / editor noise
.DS_Store
.vscode/
.idea/

# Secrets — NEVER commit these (we will learn proper secret handling later)
.env
*.key
credentials.json
